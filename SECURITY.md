<!-- Copyright The SimpleKernel Contributors -->

# 安全政策

## 支持范围

SimpleKernel 是操作系统学习项目。安全问题包括但不限于：

- 可能导致任意内存读写、权限边界绕过或不受控执行的缺陷。
- `unsafe` 不变量错误、并发竞态、页表权限错误、设备 MMIO 映射错误。
- 供应链问题，例如第三方固件、submodule、crate 或构建脚本的来源和版本风险。
- CI、发布、外部交付物或构建流程中的凭证泄露风险。

## 报告方式

请不要在公开 Issue 中披露可利用细节。优先使用 GitHub Security Advisory 的私密报告入口；如果仓库未启用该入口，请联系维护者并只提供最小复现信息。

报告建议包含：

- 受影响版本、commit 或分支。
- 影响模块或路径。
- 复现步骤、输入、日志和预期影响。
- 是否涉及真实凭证、私有数据、外部交付物或硬件批次。
- 已尝试的缓解方式。

## 处理流程

1. 维护者确认影响范围和复现条件。
2. 必要时创建私有修复分支或安全公告。
3. 修复必须包含验证命令；涉及架构不变量时补充 ADR 或设计文档。
4. 发布修复后再公开细节。

## 密钥与凭证

- 不提交真实密钥、token、私有 URL、生产数据或外部保密资料。
- 示例配置只能使用占位值。
- 日志和 panic 信息不得输出凭证或私有数据。
- 需要记录外部交付物或非公开信息时，优先使用内部受控系统；如必须写入仓库，应落到相关 ADR、设计或审计文档，并标明可公开边界。
