# `{crate_name}` — {一句话描述}

> **所属层级**：{R1 原语层 / R2 同步层 / R3 内存子系统 / R4 架构层 / ...}
>
> **依赖方向**：{列出本 crate 的直接依赖和被谁依赖}

## 架构设计

### 在系统中的位置

{说明本模块在内核整体架构中的角色，与上下游模块的关系。}

{如有 Mermaid 架构图，放在此处。}

### 设计决策

{列出本模块的关键设计选择及其理由。引用 ADR 编号（如有）。}

| 决策 | 选择 | 理由 | ADR |
|------|------|------|-----|
| {例：状态管理方式} | {例：typestate} | {例：编译期防止非法状态转换} | {ADR-xxx} |

### 参考

{借鉴了哪些外部内核/论文的设计，标注出处。}

- {例：`[Theseus: MappedPages]` — RAII 映射管理}
- {例：`[Linux: mm/page_alloc.c]` — buddy 分配算法}

## 模块细节

### 公共 API

{列出本 crate 导出的主要类型和函数，简要说明用途。}

| 类型/函数 | 用途 |
|-----------|------|
| `{TypeName}` | {描述} |
| `{fn_name}()` | {描述} |

### 内部设计

{描述内部实现的关键数据结构、算法、状态机等。}

{如有状态机，用 Mermaid state diagram 表示。}

### Safety 说明

{本 crate 中 `unsafe` 的使用情况和安全不变量。}

| unsafe 用途 | 位置 | 安全不变量 |
|-------------|------|-----------|
| {例：裸指针解引用} | `src/xxx.rs:42` | {例：指针由 frame allocator 保证有效} |

### 错误处理

{本 crate 的错误类型和处理策略（`Result` vs `panic`）。}

## 测试

{现有测试覆盖情况，以及已知的测试空白。}

| 测试类型 | 文件 | 覆盖范围 |
|----------|------|----------|
| 单元测试 | `src/xxx.rs` | {描述} |
| 系统测试 | `tests/system/xxx_tests.rs` | {描述} |

## 依赖

```
{从 cargo metadata 提取的依赖树片段}
```
