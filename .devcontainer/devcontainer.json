{
	"name": "simplekernel-devcontainer",
	"build": {
		"dockerfile": "Dockerfile",
		"options": [
			"--tag",
			"simplekernel-devcontainer:latest"
		]
	},
	"workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
	"workspaceFolder": "/workspace",
	"runArgs": [
		"--name",
		"${localEnv:DEVCONTAINER_NAME}"
	],
	"remoteUser": "dev",
	"updateRemoteUserUID": true,
	"containerEnv": {
		"CARGO_HOME": "/usr/local/cargo",
		"RUSTUP_HOME": "/usr/local/rustup",
		"CARGO_TARGET_DIR": "${containerWorkspaceFolder}/target",
		"TMPDIR": "${containerWorkspaceFolder}/.tmp"
	},
	"postCreateCommand": "mkdir -p ${containerWorkspaceFolder}/target ${containerWorkspaceFolder}/.tmp && git config --global --add safe.directory ${containerWorkspaceFolder} && git submodule update --init --recursive --depth 1 && pre-commit install --install-hooks && rustup show && cargo fetch --locked && cargo --version && cargo clippy --version && rustfmt --version && qemu-system-riscv64 --version && qemu-system-aarch64 --version && mkimage -V && gh --version && pre-commit --version && mmdc --version",
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.defaultProfile.linux": "zsh",
				"rust-analyzer.check.command": "clippy",
				"rust-analyzer.check.targets": [
					"riscv64gc-unknown-none-elf",
					"aarch64-unknown-none"
				]
			},
			"extensions": [
				"rust-lang.rust-analyzer",
				"ms-vscode.hexeditor",
				"plorefice.devicetree",
				"zixuanwang.linkerscript",
				"dan-c-underwood.arm",
				"zhwu95.riscv",
				"GitHub.vscode-pull-request-github",
				"eamodio.gitlens"
			]
		}
	}
}
