# Copyright The SimpleKernel Contributors

[package]
name = "arch_primitives"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Architecture primitives — CPU registers, interrupt state, TLB maintenance, paging geometry, and arch-specific constants"
edition.workspace = true

[dependencies]
config = { path = "../config" }

# RISC-V（仅裸机目标）
[target.'cfg(all(target_os = "none", target_arch = "riscv64"))'.dependencies]
riscv.workspace = true

# AArch64（仅裸机目标）
[target.'cfg(all(target_os = "none", target_arch = "aarch64"))'.dependencies]
aarch64-cpu.workspace = true
tock-registers.workspace = true

[lints]
workspace = true
