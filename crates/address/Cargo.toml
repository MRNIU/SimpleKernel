[package]
name = "address"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Type-safe physical/virtual address and page number abstractions for SimpleKernel"
edition.workspace = true

[dependencies]
config = { path = "../config" }
