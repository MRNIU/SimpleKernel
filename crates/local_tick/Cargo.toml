[package]
name = "local_tick"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Per-CPU tick counter for scheduler accounting"
edition.workspace = true

[dependencies]
per_cpu = { path = "../per_cpu" }
